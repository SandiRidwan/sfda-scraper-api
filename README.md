# sfda-scraper-api
Production REST API scraping 8,760 drug records from Saudi Food &amp; Drug Authority (SFDA). Bypasses TLS fingerprinting (WinError 10054) using curl_cffi Chrome impersonation. Hidden POST pagination discovered via Playwright intercept. FastAPI + Docker + SQLite + Telegram alerts.
